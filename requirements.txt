# TingLan (听澜) GUI Dependencies
PySide6==6.6.0
pyshark>=0.6
jinja2>=3.1.0
pyahocorasick
# 内存看护用（core/memory_guard.py）。缺失时看护静默降级为"永远 ok"，
# 分析照常跑，只是大包时没有内存告警和自动降级保护。
psutil>=5.9.0

# MCP Server Dependencies
# 1.x 和 2.x 都能跑：2.0 把 FastMCP 改名成 MCPServer 并换了模块路径，
# mcp_server.py 里按顺序试两条导入路径。
mcp>=1.0.0
pycryptodome>=3.20.0
